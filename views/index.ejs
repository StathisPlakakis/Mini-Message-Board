<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <a href="/new">Post message</a>
    <% messages.forEach((message) => { %>
      <div>
        <h1><%= message.user %></h1>
        <p><%= message.text %></p>
        <p><%= message.added %></p>
      </div>
    <% }); %>
  </body>
</html>